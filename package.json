{
  "scripts": {
    "build": "npm --prefix backend ci --cache backend/.npm-cache --no-audit --no-fund && npm --prefix frontend ci --cache frontend/.npm-cache --no-audit --no-fund && npm --prefix frontend run build"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "leaflet": "^1.9.4",
    "lucide-react": "^0.563.0",
    "react-leaflet": "^5.0.0"
  }
}
